# Praktikum 1 - Pengenalan Dart

Mengenalkan struktur project Dart, fungsi `main`, variabel, string interpolation, konversi tipe data, dan `dart run`.

```powershell
dart run
dart run bin/pengenalan.dart
```
